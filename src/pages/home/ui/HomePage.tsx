import { Container } from "@/shared/ui";
import { ProductList } from "@/widgets/product-list";

export const HomePage = () => {
  return (
    <Container>
      <ProductList />
    </Container>
  );
};
